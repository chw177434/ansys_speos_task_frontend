# Direct 模式断点续传 - 前后端接口一致性分析

## 📋 完整流程分析

### 当前前端流程（Direct 模式断点续传）

```
1. 用户选择文件（master + include）
   ↓
2. 检查 localStorage 中是否有未完成的上传
   ↓
3. 上传 Master 文件（分片）
   - 调用: initDirectMultipartUpload
   - 返回: task_id (master), upload_id, file_path
   ↓
4. 上传 Include 文件（分片，如果有）
   - 调用: initDirectMultipartUpload (传递 master 的 task_id)
   - 返回: task_id (include), upload_id, file_path
   ↓
5. 提交任务
   - 调用: submitDirectUpload (方式2: 基于已上传文件)
   - 传递: task_id (使用 master 的 task_id)
   - 后端: 查找文件并解压 include
```

---

## 🔍 发现的问题

### 问题 1: task_id 参数支持不明确

**前端实现**：
- `DirectMultipartInitRequest` 接口已添加 `task_id?: string` 参数
- `DirectResumableUploadManager` 在初始化时会传递 `task_id`（如果存在）

**后端需要确认**：
- ✅ `/api/upload/direct/multipart/init` 接口是否支持 `task_id` 参数？
- ✅ 如果支持，传递 `task_id` 时，是否使用该 `task_id` 而不是创建新的？
- ✅ 如果不支持，后端会如何处理？是否忽略该参数？

**当前行为**：
- 前端传递了 `task_id`，但后端可能返回了不同的 `task_id`
- 这说明后端可能不支持该参数，或者逻辑有问题

---

### 问题 2: 文件存储位置不一致

**后端日志显示**：
```
Master 文件: /home/hongwei/speos_data/inputs/84cbb673-390b-4cab-8254-5364ac8b0708/SPEOS_input_files_2_.zip
提交任务时查找: /home/hongwei/speos_data/inputs/aed27a97-cfef-4b99-974f-d3b7dce677b6/
```

**问题**：
- Master 文件存储在 `task_id = 84cbb673-390b-4cab-8254-5364ac8b0708` 目录
- 提交任务时使用 `task_id = aed27a97-cfef-4b99-974f-d3b7dce677b6`
- 两个 task_id 不一致，导致找不到文件

**后端已实现跨目录查找**：
- 后端会在其他目录中查找最近 5 分钟内上传的文件
- 但这不是最佳实践，应该确保 task_id 一致

---

### 问题 3: submit-direct 接口参数确认

**前端实现（方式2）**：
```typescript
{
  task_id: "master-task-id",
  profile_name: "...",
  version: "...",
  job_name: "...",
  // ... 其他参数
  // 不传递 master_file 和 include_file
}
```

**后端需要确认**：
- ✅ 当传递 `task_id` 时，是否必须不传递 `master_file` 和 `include_file`？
- ✅ 后端查找文件的逻辑：
  - Master 文件查找位置：`{INPUT_DIR}/{task_id}/` 或 `{TMP_DIR}/{task_id}/`
  - Include 压缩包查找位置：`{TMP_DIR}/{task_id}/` 或 `{INPUT_DIR}/{task_id}/_tmp`
- ✅ 如果找不到文件，错误信息是什么？

---

## 📡 接口规范（需要前后端一致）

### 1. 初始化分片上传

**接口**: `POST /api/upload/direct/multipart/init`

**前端请求**:
```typescript
{
  filename: string;
  file_size: number;
  file_type: "master" | "include";
  chunk_size?: number;
  task_id?: string;  // ⚠️ 需要后端确认是否支持
}
```

**后端需要确认**:
- [ ] 是否支持 `task_id` 参数？
- [ ] 如果支持，传递 `task_id` 时的行为：
  - 使用该 `task_id` 还是创建新的？
  - 如果该 `task_id` 已存在，是否复用？
- [ ] 如果不支持，是否忽略该参数？

**后端响应**:
```typescript
{
  task_id: string;  // ⚠️ 如果传递了 task_id，返回的应该与传递的一致
  upload_id: string;
  total_chunks: number;
  parts: Array<{...}>;
}
```

---

### 2. 完成分片上传

**接口**: `POST /api/upload/direct/multipart/complete`

**前端请求**:
```typescript
{
  task_id: string;
  upload_id: string;
  filename: string;
  file_type: "master" | "include";
  parts: Array<{ part_number: number }>;
}
```

**后端响应**:
```typescript
{
  message: string;
  file_path: string;  // ⚠️ 文件存储的完整路径
}
```

**后端需要确认**:
- [ ] 文件存储位置规则：
  - Master 文件：`{INPUT_DIR}/{task_id}/{filename}` 或 `{TMP_DIR}/{task_id}/{filename}`？
  - Include 文件：`{TMP_DIR}/{task_id}/{filename}` 或 `{INPUT_DIR}/{task_id}/_tmp/{filename}`？
- [ ] 如果 `task_id` 不同，文件会存储在哪里？

---

### 3. 提交任务（方式2：基于已上传文件）

**接口**: `POST /api/tasks/submit-direct`

**前端请求（方式2）**:
```typescript
FormData {
  task_id: string;  // ⚠️ Master 文件的 task_id
  profile_name: string;
  version: string;
  job_name: string;
  // ... 其他参数
  // 不包含 master_file 和 include_file
}
```

**后端需要确认**:
- [ ] 文件查找逻辑：
  1. Master 文件查找顺序：
     - `{INPUT_DIR}/{task_id}/` 目录下的第一个文件？
     - `{TMP_DIR}/{task_id}/` 目录下的第一个文件？
     - 如何识别 master 文件？
  
  2. Include 压缩包查找顺序：
     - `{TMP_DIR}/{task_id}/` 目录下查找 `.zip`, `.rar`, `.7z` 等？
     - `{INPUT_DIR}/{task_id}/_tmp/` 目录下查找？
     - `{INPUT_DIR}/{task_id}/` 目录下查找？
     - 如果 task_id 不一致，跨目录查找的范围是什么？
  
  3. 跨目录查找逻辑：
     - 查找最近 5 分钟内上传的文件？
     - 查找范围：整个 `INPUT_DIR` 还是只查找 `TMP_DIR`？
     - 如何匹配文件？通过文件名、大小、还是时间戳？

- [ ] 如果找不到文件，错误信息是什么？
- [ ] Include 文件解压位置：
  - 解压到 `{INPUT_DIR}/{task_id}/` 目录？
  - 是否跳过与 master 文件同名的文件？

---

## ✅ 前端当前实现状态

### 已实现的功能

1. ✅ 断点续传支持（localStorage 保存进度）
2. ✅ 分片上传（自动分片，支持大文件）
3. ✅ 进度监控（实时显示上传进度、速度、剩余时间）
4. ✅ 错误处理（详细的错误日志和用户提示）
5. ✅ 取消上传（支持 AbortController）
6. ✅ Include 文件强制使用 master 的 task_id（如果 master 已上传）

### 待确认的问题

1. ⚠️ 后端是否支持 `task_id` 参数（在 init 接口中）？
2. ⚠️ 如果后端不支持，前端如何处理？
3. ⚠️ 文件存储位置规则是否与后端一致？
4. ⚠️ 跨目录查找逻辑是否可靠？

---

## 🔧 后端需要配合的信息

### 1. 接口参数支持确认

请后端明确回答：

**问题 A**: `/api/upload/direct/multipart/init` 接口是否支持 `task_id` 参数？

- [ ] 支持：如果传递 `task_id`，使用该 `task_id`（不创建新的）
- [ ] 不支持：忽略该参数，总是创建新的 `task_id`
- [ ] 部分支持：如果 `task_id` 已存在且匹配，复用；否则创建新的

**问题 B**: 如果支持 `task_id` 参数，include 文件传递 master 的 `task_id` 时：
- [ ] 是否允许同一个 `task_id` 下有多个文件（master + include）？
- [ ] 如何区分 master 和 include 文件？通过 `file_type` 参数？

---

### 2. 文件存储位置规则

请后端明确说明：

**Master 文件存储位置**：
```
规则1: {INPUT_DIR}/{task_id}/{filename}
规则2: {TMP_DIR}/{task_id}/{filename}
规则3: 其他规则？
```

**Include 文件存储位置**：
```
规则1: {TMP_DIR}/{task_id}/{filename}
规则2: {INPUT_DIR}/{task_id}/_tmp/{filename}
规则3: {INPUT_DIR}/{task_id}/{filename}
规则4: 其他规则？
```

**当前实际存储位置**（从日志看）：
- Master: `/home/hongwei/speos_data/inputs/{task_id}/{filename}`
- Include: 需要确认

---

### 3. submit-direct 接口（方式2）文件查找逻辑

请后端明确说明：

**Master 文件查找**：
1. 首先在 `{INPUT_DIR}/{task_id}/` 目录下查找
2. 如果找不到，在 `{TMP_DIR}/{task_id}/` 目录下查找
3. 如果还找不到，是否跨目录查找？
4. 如何识别 master 文件？第一个文件？特定命名规则？

**Include 压缩包查找**：
1. 查找位置顺序？
2. 支持的压缩包格式：`.zip`, `.rar`, `.7z`, `.tar`, `.gz`, `.tar.gz`？
3. 如果 task_id 不一致，跨目录查找的范围？
4. 跨目录查找的时间窗口（5分钟）？

**错误处理**：
- 如果找不到 master 文件，返回什么错误？
- 如果找不到 include 文件，是否继续处理（不报错）？

---

### 4. 测试建议

请后端配合测试以下场景：

**场景 1**: task_id 一致
- Master 文件上传到 `task_id = A`
- Include 文件上传到 `task_id = A`（传递 `task_id = A`）
- 提交任务时使用 `task_id = A`
- ✅ 应该能找到所有文件

**场景 2**: task_id 不一致（当前问题）
- Master 文件上传到 `task_id = A`
- Include 文件上传到 `task_id = B`（后端创建了新的）
- 提交任务时使用 `task_id = A`
- ⚠️ 后端应该通过跨目录查找找到 include 文件

**场景 3**: 只有 master 文件
- Master 文件上传到 `task_id = A`
- 没有 include 文件
- 提交任务时使用 `task_id = A`
- ✅ 应该能正常创建任务

---

## 🎯 推荐的解决方案

### 方案 1: 后端支持 task_id 参数（推荐）

**优点**：
- 确保 task_id 一致
- 文件都在同一目录
- 逻辑清晰，易于维护

**实现**：
1. 后端 `/api/upload/direct/multipart/init` 接口支持 `task_id` 参数
2. 如果传递 `task_id`，使用该 `task_id`（不创建新的）
3. 前端 include 文件上传时传递 master 的 `task_id`

### 方案 2: 后端不支持 task_id 参数（当前情况）

**优点**：
- 不需要修改后端接口
- 后端已有跨目录查找逻辑

**缺点**：
- task_id 不一致，需要跨目录查找
- 逻辑复杂，可能出错

**实现**：
1. 前端接受 task_id 不一致的情况
2. 提交任务时使用 master 的 `task_id`
3. 后端通过跨目录查找找到 include 文件

---

## 📝 总结

### 前端当前状态
- ✅ 代码实现完整
- ✅ 错误处理完善
- ⚠️ 依赖后端接口支持

### 需要后端确认
1. **`task_id` 参数支持**：是否支持？如何行为？
2. **文件存储位置**：明确的存储规则
3. **文件查找逻辑**：详细的查找顺序和规则
4. **错误处理**：找不到文件时的错误信息

### 下一步行动
1. 后端明确回答上述问题
2. 根据后端回答，调整前端代码（如果需要）
3. 进行完整测试，确保所有场景正常工作

---

**最后更新**: 2025-11-14  
**文档版本**: 1.0

