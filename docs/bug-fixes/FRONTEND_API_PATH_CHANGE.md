# 前端 API 路径变更通知

## 🚨 重要变更

**所有任务相关接口路径已统一加上 `/api` 前缀**

---

## 📝 需要修改的路径

### 搜索并替换（推荐）

在前端项目中全局搜索替换：

| 搜索字符串 | 替换为 | 数量（估计） |
|-----------|--------|------------|
| `"/tasks"` | `"/api/tasks"` | ~5 处 |
| `'/tasks'` | `'/api/tasks'` | ~5 处 |
| ``/tasks`` | ``/api/tasks`` | ~5 处 |

**工具：** VSCode 全局搜索替换（Ctrl+Shift+H）

---

## ✅ 完整对照表

| 功能 | 旧路径 → 新路径 |
|------|----------------|
| 任务列表 | `/tasks` → `/api/tasks` |
| 任务详情 | `/tasks/{id}` → `/api/tasks/{id}` |
| 删除任务 | `/tasks/{id}` → `/api/tasks/{id}` |
| 初始化上传 | `/tasks/init-upload-v2` → `/api/tasks/init-upload-v2` |
| 确认上传 | `/tasks/confirm-upload-v2` → `/api/tasks/confirm-upload-v2` |
| Direct 上传 | `/tasks/submit-direct` → `/api/tasks/submit-direct` |
| 输出文件列表 | `/tasks/{id}/outputs` → `/api/tasks/{id}/outputs` |
| 下载结果 | `/tasks/{id}/download` → `/api/tasks/{id}/download` |

---

## ⚠️ 注意

### 不要替换

- ❌ 页面路由（如 `<Link to="/tasks">`）
- ❌ 路由配置（如 `path: "/tasks"`）
- ✅ 只替换 API 调用路径（fetch, axios 等）

---

## 🧪 快速验证

**修改后测试：**

```bash
# 如果这些接口返回 404，说明路径没改对
GET  /api/tasks
GET  /api/tasks/{id}
POST /api/tasks/submit-direct
```

---

## 📞 联系

如有疑问，查看：`docs/API_PATHS_UNIFIED.md`

---

**生效时间：** 立即  
**影响范围：** 所有任务相关接口  
**修改工作量：** 约 10 分钟（全局搜索替换）

